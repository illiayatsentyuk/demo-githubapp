"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("@nestjs/common");
const crypto_1 = require("crypto");
const github_webhook_constants_1 = require("../github-webhook.constants");
const core_1 = require("@nestjs/core");
let GithubGuard = class GithubGuard {
    constructor(githubWebhookModuleOptions, reflector) {
        this.githubWebhookModuleOptions = githubWebhookModuleOptions;
        this.reflector = reflector;
    }
    canActivate(context) {
        const restrictedEvents = this.reflector.get(github_webhook_constants_1.GITHUB_WEBHOOK_EVENTS, context.getHandler());
        const request = context.switchToHttp().getRequest();
        const signature = request.headers['x-hub-signature'];
        const githubEvent = request.headers['x-github-event'];
        if (!signature) {
            throw new common_1.UnauthorizedException(`This request doesn't contain a github signature`);
        }
        if (githubEvent &&
            restrictedEvents &&
            restrictedEvents.length > 0 &&
            restrictedEvents.find(event => event.toLowerCase() === githubEvent.toLowerCase()) === undefined) {
            throw new common_1.UnauthorizedException(`An unsupported webhook event was triggered`);
        }
        if (!this.githubWebhookModuleOptions.webhookSecret) {
            console.log(`There is no webhook secret set`);
            throw new common_1.UnauthorizedException();
        }
        return this.calculateSignature(signature, request.body, this.githubWebhookModuleOptions.webhookSecret);
    }
    calculateSignature(signature, payload, secret) {
        const hmac = crypto_1.createHmac('sha1', secret);
        const digest = 'sha1=' + hmac.update(JSON.stringify(payload)).digest('hex');
        if (!signature || !digest || signature !== digest) {
            throw new common_1.UnauthorizedException(`Request body digest (${digest}) does not match ${signature}`);
        }
        return true;
    }
};
GithubGuard = __decorate([
    common_1.Injectable(),
    __param(0, common_1.Inject(github_webhook_constants_1.GITHUB_WEBHOOKS_OPTIONS)),
    __metadata("design:paramtypes", [Object, core_1.Reflector])
], GithubGuard);
exports.GithubGuard = GithubGuard;

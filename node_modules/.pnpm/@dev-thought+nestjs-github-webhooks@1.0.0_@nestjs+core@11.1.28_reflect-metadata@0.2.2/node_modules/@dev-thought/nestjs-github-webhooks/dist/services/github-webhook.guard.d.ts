import { CanActivate, ExecutionContext } from '@nestjs/common';
import { Observable } from 'rxjs';
import { GithubWebhookModuleOptions } from '../interfaces/github-webhook-module-options.interface';
import { Reflector } from '@nestjs/core';
export declare class GithubGuard implements CanActivate {
    private githubWebhookModuleOptions;
    private readonly reflector;
    constructor(githubWebhookModuleOptions: GithubWebhookModuleOptions, reflector: Reflector);
    canActivate(context: ExecutionContext): boolean | Promise<boolean> | Observable<boolean>;
    private calculateSignature;
}

"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var GithubWebhooksModule_1;
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("@nestjs/common");
const github_webhook_constants_1 = require("./github-webhook.constants");
const github_webhook_guard_1 = require("./services/github-webhook.guard");
let GithubWebhooksModule = GithubWebhooksModule_1 = class GithubWebhooksModule {
    static forRoot(options) {
        return {
            module: GithubWebhooksModule_1,
            providers: [
                {
                    provide: github_webhook_constants_1.GITHUB_WEBHOOKS_OPTIONS,
                    useValue: options,
                },
            ],
        };
    }
    static forRootAsync(options) {
        return {
            module: GithubWebhooksModule_1,
            imports: options.imports,
            providers: [...this.createAsyncProviders(options)],
        };
    }
    static createAsyncProviders(options) {
        if (options.useExisting || options.useFactory) {
            return [this.createAsyncOptionsProvider(options)];
        }
        return [
            this.createAsyncOptionsProvider(options),
            {
                provide: options.useClass,
                useClass: options.useClass,
            },
        ];
    }
    static createAsyncOptionsProvider(options) {
        if (options.useFactory) {
            return {
                provide: github_webhook_constants_1.GITHUB_WEBHOOKS_OPTIONS,
                useFactory: (...args) => __awaiter(this, void 0, void 0, function* () { return yield options.useFactory(...args); }),
                inject: options.inject || [],
            };
        }
        return {
            provide: github_webhook_constants_1.GITHUB_WEBHOOKS_OPTIONS,
            useFactory: (optionsFactory) => __awaiter(this, void 0, void 0, function* () { return yield optionsFactory.createWebhookOptions(); }),
            inject: [options.useExisting || options.useClass],
        };
    }
};
GithubWebhooksModule = GithubWebhooksModule_1 = __decorate([
    common_1.Module({
        providers: [github_webhook_guard_1.GithubGuard],
        exports: [github_webhook_guard_1.GithubGuard, github_webhook_constants_1.GITHUB_WEBHOOKS_OPTIONS],
    })
], GithubWebhooksModule);
exports.GithubWebhooksModule = GithubWebhooksModule;
